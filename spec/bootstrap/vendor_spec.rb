# frozen_string_literal: true

RSpec.describe Bootstrap::Vendor do
  it "has a version number" do
    expect(Bootstrap::Vendor::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
